 What is application.properties in Spring Boot?
application.properties is a file used to configure your Spring Boot application.

It lets you set things like:

Server port

Database credentials

Logging levels

Custom app variables

Spring-specific settings (e.g., JPA, security, etc.)
---------------------------------------------------


💡 Equivalent in Node.js?
In Node.js, this is most similar to:

✅ .env file + config files (like config.js, config.json)

--------------------------------------------------

🧠 Bonus: application.yml
You can also use application.yml instead of .properties — it’s the same, just a different syntax:

server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: 1234



